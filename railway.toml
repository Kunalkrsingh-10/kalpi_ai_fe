[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# NEXT_PUBLIC_API_BASE_URL is set as an env var in Railway dashboard
# Railway passes it automatically as a Docker build ARG

[deploy]
numReplicas         = 1
restartPolicyType   = "ON_FAILURE"
restartPolicyMaxRetries = 10
